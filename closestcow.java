import java.util.Scanner;

class ClosestCow {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        int currentLine = 0;

        while (scanner.hasNextLine()) {
            currentLine += 1;  
            String line = scanner.nextLine();
            int K, M, N;
         
          if (currentLine == 1) {
            parseInt()
          } else if (currentLine < 8) {
            
          }
            // Integer arrays for the patchLocations, patchTastiness, cowLocations
            int[] patchLocs = {0, 4, 8, 10, 12, 13}; // (Left column of inputs below the first line)
      }
        

        scanner.close();
    }

    void solutionMethod() { // may be a separate (probably will be) a different return type instead of void 
        // Solution work here 
      }
}
